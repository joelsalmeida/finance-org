# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build finance-back`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t finance-back`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system finance-back && \
          adduser --system -G finance-back finance-back

COPY dist/apps/finance-back finance-back/
RUN chown -R finance-back:finance-back .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix finance-back --omit=dev -f install

CMD [ "node", "finance-back" ]
